
{{#each comment}}
    <div class="row mb-4">
        <div class="col-md-5">
            <h4 class="username">{{user.username}} on {{format_date date_created}}</h4>
        </div>
        <div class="col-md-7">
            <p>{{content}}</p>
        </div>
    </div>
{{/each}}
